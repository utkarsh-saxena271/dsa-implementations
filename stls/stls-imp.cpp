#include <iostream>
#include <vector>
#include <deque>
#include <list>
#include <stack>
#include <queue>


#include <map>
using namespace std;

int main(){
    // vector<datatype> name(size, val)
    // list<datatype> name // push_back, push_front, pop_back, pop_front // doubly linked list
    // deque<datatype> name // push_back, push_front, pop_back, pop_front //double ended queue
    // pair<datatype1, datatype2> name //.first, .second


    // stack<datatype> name // push(), top(), pop(), front(), empty()
    // queue<datatype> name // push(), front(), pop(), empty()
    // priority_queue<datatype> name //push(), top(), pop() //largest(ascending)
    // priority_queue<int, vector<int>, greater<int>> name //push(), top(), pop() //smallest(descending)

    // map<datatype1, datatype2> name // unique keys, insert(), count(), erase(), find(), empty()

    return 0;
}